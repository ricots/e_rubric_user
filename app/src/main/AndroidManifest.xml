<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unikama.acer.e_rubic">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/classroom"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activity.splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.penilaian" />
        <activity android:name=".fragment.history" />
        <activity android:name=".activity.detail_histori" />
        <activity android:name=".activity.masuk" />
        <activity
            android:name=".activity.daftar"
            android:label="E-RUBRIC" />
        <activity android:name=".activity.update_penilaian"></activity>
    </application>

</manifest>